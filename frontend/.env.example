# Cloudinary 設定
# 請從 Cloudinary Dashboard 取得這些資訊
# https://cloudinary.com/console

# Cloud Name (必填)
# 在 Dashboard 頂部可以找到
VITE_CLOUDINARY_CLOUD_NAME=dfaittd9e

# Upload Preset (必填) 
# 需要在 Settings > Upload > Upload presets 建立
# 選擇 "Unsigned" 模式以允許前端直接上傳
VITE_CLOUDINARY_UPLOAD_PRESET=pangcah_unsigned

# API URL (選填，後端使用)
VITE_API_URL=http://localhost:8000