{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend-django && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd backend-django && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn family_finance.wsgi:application",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}